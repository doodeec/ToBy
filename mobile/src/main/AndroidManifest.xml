<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.doodeec.toby" >

    <application
        android:name=".ApplicationState.AppState"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Presentation.ShoppingItem.ShoppingItemListActivity"
            android:label="@string/title_activity_shopping_item_list" >
        </activity>
        <activity
            android:name=".Presentation.ShoppingList.ShoppingListListActivity"
            android:label="@string/title_activity_shopping_list_list" >
        </activity>
    </application>

</manifest>
